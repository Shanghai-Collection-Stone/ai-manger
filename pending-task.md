# 待办任务 (Pending Tasks)

## 租户入驻（暂不做）

当前 `POST /admin/auth/register` **不包含入驻流程**：自助注册一律进默认租户「其他」；租户入驻是独立入口，桌面端入驻页经 `GET /admin/auth/sales-contact` 展示平台后台配置的业务员微信二维码（后台「平台AI配置」Tab，仅超管可设置），由业务员线下对接后在「租户管理」手动建租户，再由租户管理员按手机号添加成员。

- [ ] 入驻申请：提交企业名称 / 联系人 / 手机号 → 平台审核 → 自动创建租户 + 首个 `tenant_admin`
- [ ] 入驻申请列表与审核界面（后台）
- [ ] 业务员分配：按地区 / 行业返回不同业务员二维码（当前为全平台一个）
- [ ] 未入驻线索留存：注册已要求短信验证，可把「已验证手机号 + 填写的租户名称」记为销售线索（当前未落库）

## 注册相关遗留

- [x] 注册页面：已在 xhs-manger 桌面端实现（`src/xhs-desktop/register.*`，登录页「立即注册」入口，经主进程调用注册与发码接口，未入驻展示业务员二维码）
- [ ] ai-manger 网页端登录页暂无注册入口（如需要可复用 `web/src/ui/SmsVerification/SmsCodeInput.jsx` + `adminApi.register`）
- [ ] 后台「用户管理」列表显示手机号，方便租户管理员识别并启用自助注册的账号
- [x] 注册审核：自助注册账号改为进默认租户「其他」、`enabled=true` 注册即可登录
- [x] 同一手机号重复注册：`admin_accounts.phone` 唯一，重复注册返回 `PHONE_ALREADY_REGISTERED`
- [ ] 租户名称唯一约束：默认租户「其他」重名时注册返回 `SELF_REGISTER_TENANT_AMBIGUOUS`，建议 `sass_tenants.name` 加唯一索引（顺带消除并发首次注册重复建「其他」的窗口）
- [ ] 后台「用户管理」新建成员表单加手机号字段（后端 `CreateAdminUserDto.phone` 已支持按手机号关联账号，前端 AdminApp 未接）
- [ ] 找回密码 / 修改密码：已关联账号的密码只有超管能在后台重置，需要本人通过短信验证码自助改密（新增 SMS 场景）
- [ ] 登录接口限流：`/admin/auth/login/identify` 与旧 `/admin/auth/login` 目前无失败次数限制
- [ ] 登录后切换租户：目前需退出重新登录选择，可在账户中心加「切换租户」
- [ ] 老账号绑定手机号：无手机号的历史账号仍按用户名登录，后续可在登录后引导短信绑定
- [ ] 启动迁移口径：同一手机号的多条历史成员会共用该号「最近登录」那一行的密码，上线前确认受影响账号

## 短信验证码遗留

- [ ] 发码接口防刷加强：图形验证码 / 滑块（当前只有手机号 60 秒间隔、单号日 10 次、单 IP 小时 30 次）
- [ ] 客户端 IP 可信来源：当前取 `X-Forwarded-For` 首段，可被伪造绕过 IP 限额；部署在反向代理后应配置 `trust proxy` 并只信任代理写入的地址
- [ ] 其他短信服务商（腾讯云等），`SmsProvider` 目前只有 `aliyun`
- [ ] 同一验证码的并发请求：作废在业务成功之后，极端并发下同一验证码可能让两个请求都通过（例如注册两个不同用户名）
- [ ] 其他需要验证码的场景（找回密码、换绑手机号、敏感操作二次确认）按需在 `SMS_VERIFICATION_SCENES` 登记并挂 `@RequireSmsCode`
