# 任务专项接口文档（Task API）

## 概述

此接口专供 `claw skill` 使用，通过 `taskToken` 进行鉴权，无需管理员登录态。

**基础路径**: `http://127.0.0.1:3011`

**鉴权方式**: 请求头 `Authorization: Bearer <taskToken>`

---

## 核心鉴权规则

| 规则 | 说明 |
|------|------|
| 每个接口必须携带 `Authorization: Bearer <taskToken>` | 无此 header 返回 401 |
| `taskToken` 与路径中的 `todoId` 必须完全匹配 | 不匹配返回 401 `TASK_TOKEN_MISMATCH` |
| taskToken 一经创建绑定任务不可更换 | 通过任务创建接口（非 task-api）获取 |
| 跨任务操作被拒绝 | 每个 token 只能操作绑定的 todoId |

---

## 通用状态规则

### 任务状态（status）

合法值: `pending` / `in_progress` / `done` / `failed` / `cancelled`

**别名归一化（AI 传入别名时自动转换）**:

| 别名 | 归一化为 |
|------|---------|
| `completed` / `finish` / `finished` / `success` / `succeed` | `done` |
| `running` / `started` / `processing` | `in_progress` |
| `error` | `failed` |
| `abort` / `aborted` / `cancel` | `cancelled` |

- 传入非合法值（含中文状态）时，该字段自动忽略（不写库）
- 通过 `task-api` 创建的执行节点，`status` 默认为 `in_progress`

---

## 通用错误码

| 错误信息 | HTTP 状态码 | 描述 |
|---------|-------------|------|
| `TASK_TOKEN_REQUIRED` | 401 | 未提供 Authorization header 或 Bearer token |
| `INVALID_TASK_TOKEN` | 401 | taskToken 无效或已过期 |
| `TASK_TOKEN_MISMATCH` | 401 | taskToken 与路径 todoId 不匹配 |
| `ITEM_NOT_BELONG_TO_TASK` | 401 | 执行节点不属于当前任务 |
| `STAT_NOT_BELONG_TO_TASK` | 401 | XHS 帖子数据不属于当前任务 |

---

## 获取 Canvas 文章列表

获取该任务关联的 Canvas 下的所有文章，图片地址已转换为完整路径。

### 请求

```
GET /task-api/:todoId/canvas
Authorization: Bearer <taskToken>
```

### 路径参数

| 参数 | 类型 | 描述 |
|------|------|------|
| `todoId` | number | 任务 ID |

### 响应

```json
{
  "canvas": {
    "id": 1,
    "topic": "画布主题",
    "status": "completed",
    "createdAt": "2026-04-01T00:00:00.000Z",
    "updatedAt": "2026-04-01T00:00:00.000Z"
  },
  "articles": [
    {
      "id": 1,
      "title": "文章标题",
      "tags": ["标签1", "标签2"],
      "contentJson": {},
      "imageUrls": [
        "http://127.0.0.1:3011/uploads/image1.png",
        "http://127.0.0.1:3011/uploads/image2.png"
      ],
      "imageIds": [],
      "status": "done",
      "doneNote": "完成备注"
    }
  ]
}
```

### 响应字段说明

| 字段 | 类型 | 描述 |
|------|------|------|
| `canvas` | object | 画布基本信息（id/topic/status/createdAt/updatedAt），未找到时为 `null` |
| `canvas.id` | number | 画布 ID |
| `canvas.topic` | string | 画布主题 |
| `canvas.status` | string | 画布状态：`generating`/`completed`/`requires_human`/`failed` |
| `articles` | array | 文章列表 |
| `articles[].id` | number | 文章序号 ID |
| `articles[].title` | string | 文章标题 |
| `articles[].tags` | string[] | 文章标签 |
| `articles[].contentJson` | object | 文章内容（JSON 结构） |
| `articles[].imageUrls` | string[] | **完整路径**的图片地址列表 |
| `articles[].imageIds` | number[] | 图片资源 ID 列表 |
| `articles[].status` | string | 文章状态：`pending`/`done`/`requires_human`/`failed` |
| `articles[].doneNote` | string | 完成备注 |

### 图片地址转换规则

- 相对路径（如 `/uploads/image.png`）会自动拼接 `TASK_API_BASE_URL`（默认 `http://127.0.0.1:3011`）
- 完整路径（http/https/data: 开头）保持不变

### Canvas ID 提取规则

Canvas ID 从任务文本字段中自动提取，支持以下格式：

- `canvasId: 123`
- `canvas id: 123`
- `canvas: 123`
- `画布: 123`
- `画布id: 123`

### 错误码

| 错误信息 | HTTP 状态码 | 描述 |
|---------|-------------|------|
| `TASK_TOKEN_REQUIRED` | 401 | 未提供 Authorization header |
| `INVALID_TASK_TOKEN` | 401 | taskToken 无效 |
| `TASK_TOKEN_MISMATCH` | 403 | taskToken 与 todoId 不匹配 |
| `CANVAS_ID_NOT_FOUND_IN_TASK` | 200 | 任务文本中未找到 Canvas ID |
| `CANVAS_NOT_FOUND` | 200 | Canvas 不存在 |
| `CANVAS_SERVICE_UNAVAILABLE` | 500 | Canvas 服务不可用 |

---

## 获取任务详情

```
GET /task-api/:todoId
Authorization: Bearer <taskToken>
```

### 响应

```json
{
  "todo": {
    "id": 1,
    "title": "任务标题",
    "description": "任务描述",
    "status": "in_progress",
    "aiPlan": "执行计划",
    ...
  }
}
```

---

## 更新任务字段

```
PATCH /task-api/:todoId
Authorization: Bearer <taskToken>
Content-Type: application/json

{
  "status": "done",
  "taskResult": "# 执行结果\n这是内容"
}
```

### 支持更新的字段

| 字段 | 类型 | 描述 |
|------|------|------|
| `status` | string | 任务状态：`pending`/`in_progress`/`done`/`failed`/`cancelled` |
| `title` | string | 任务标题 |
| `description` | string | 任务描述 |
| `aiPlan` | string | AI 执行计划 |
| `abnormalReason` | string | 异常原因 |
| `taskResult` | string | 任务成果（markdown 内容） |
| `stage` | string | 当前阶段 |

---

## 列出执行节点

```
GET /task-api/:todoId/items
Authorization: Bearer <taskToken>
```

### 响应

```json
{
  "items": [
    {
      "id": 1,
      "todoId": 1,
      "title": "步骤1",
      "status": "done",
      "stage": "执行中",
      ...
    }
  ]
}
```

---

## 新增执行节点

```
POST /task-api/:todoId/items
Authorization: Bearer <taskToken>
Content-Type: application/json

{
  "title": "新步骤",
  "description": "步骤描述",
  "stage": "执行阶段"
}
```

---

## 更新执行节点

```
PATCH /task-api/:todoId/items/:itemId
Authorization: Bearer <taskToken>
Content-Type: application/json

{
  "status": "done",
  "doneNote": "完成备注"
}
```

---

## 删除执行节点

```
DELETE /task-api/:todoId/items/:itemId
Authorization: Bearer <taskToken>
```

---

## 环境变量

| 变量名 | 默认值 | 描述 |
|--------|--------|------|
| `TASK_API_BASE_URL` | `http://127.0.0.1:3011` | 任务 API 基础路径，用于拼接图片完整地址 |

---

---

## Canvas 文章接口

### 获取 Canvas 文章列表

```
GET /task-api/:todoId/canvas
Authorization: Bearer <taskToken>
```

> Canvas ID 从任务文本字段自动提取，支持以下格式：`canvasId: 123` / `canvas id: 123` / `画布: 123` / `画布id: 123`

**响应**:
```json
{
  "canvas": {
    "id": 1,
    "topic": "画布主题",
    "status": "completed",
    "createdAt": "...",
    "updatedAt": "..."
  },
  "articles": [
    {
      "id": 1,
      "title": "文章标题",
      "tags": ["标签1"],
      "contentJson": {},
      "imageUrls": ["http://127.0.0.1:3011/uploads/image1.png"],
      "imageIds": [],
      "status": "done",
      "doneNote": "完成备注"
    }
  ]
}
```

**图片地址规则**: 相对路径自动拼接 `TASK_API_BASE_URL`，完整 URL 保持不变。

**Canvas 状态值**: `generating` / `completed` / `requires_human` / `failed`
