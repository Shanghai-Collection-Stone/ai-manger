# 订单数据对接文档

> **文档版本**：v1.0  
> **更新日期**：2026年  
> **适用对象**：AI指挥官系统对接开发人员

---

## 一、概述

本文档用于指导AI指挥官系统接收我方电商系统的订单数据。我方系统会定时将订单相关的三类数据推送至贵方系统。

### 1.1 数据类型

| 数据类型 | 说明 | 推送频率 |
|---------|------|---------|
| 订单信息 | 包含订单基础信息、商品信息、手机号等 | 每5-10分钟 |
| 订单使用信息 | 订单商品的使用记录 | 每5-10分钟 |
| 订单退单信息 | 订单的退单记录 | 每5-10分钟 |

---

## 二、接口规范

### 2.1 通信协议

- **协议**：HTTP/HTTPS
- **请求方法**：POST
- **数据格式**：JSON
- **编码**：UTF-8

### 2.2 接口地址

我方会调用以下接口地址推送数据（请贵方提供实际的接口URL）：

| 数据类型 | 接口路径示例 |
|---------|-------------|
| 订单信息 | `POST https://your-domain.com/api/sync/orders` |
| 订单使用信息 | `POST https://your-domain.com/api/sync/usages` |
| 订单退单信息 | `POST https://your-domain.com/api/sync/refunds` |

### 2.3 请求头

我方发送请求时会携带以下请求头：

```
Content-Type: application/json
X-Request-ID: {唯一请求标识}
X-Timestamp: {ISO8601格式时间戳}
```

---

## 三、数据格式

### 3.1 订单信息数据

#### 请求体示例

```json
{
  "dataType": "order",
  "batchId": "batch-20240101-001",
  "timestamp": "2024-01-01T10:00:00Z",
  "orders": [
    {
      "orderNo": "ORD202401010001",
      "orderTime": "2024-01-01T09:30:00Z",
      "channelName": "抖音",
      "productName": "商品A",
      "productQuantity": 2,
      "phone": "U2FsdGVkX1+vupppZksvRf5pq5g5XjFRlipTg9+v6Y4="
    },
    {
      "orderNo": "ORD202401010002",
      "orderTime": "2024-01-01T09:35:00Z",
      "channelName": "旅行社",
      "productName": "商品B",
      "productQuantity": 1,
      "phone": "U2FsdGVkX1+vupppZksvRf5pq5g5XjFRlipTg9+v6Y5="
    }
  ]
}
```

#### 字段说明

**外层字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| dataType | string | 是 | 固定值：order |
| batchId | string | 是 | 批次ID，用于问题追踪 |
| timestamp | string | 是 | 发送时间，ISO8601格式 |
| orders | array | 是 | 订单数据数组 |

**orders数组内字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 订单号，唯一标识 |
| orderTime | string | 是 | 下单时间，ISO8601格式 |
| channelName | string | 是 | 下单渠道（如：微信小程序、APP、网页等） |
| productName | string | 是 | 商品名称 |
| productQuantity | int | 是 | 购买数量 |
| phone | string | 是 | 手机号（AES加密，见第四章） |

---

### 3.2 订单使用信息数据

#### 请求体示例

```json
{
  "dataType": "usage",
  "batchId": "batch-20240101-002",
  "timestamp": "2024-01-01T10:00:00Z",
  "usages": [
    {
      "orderNo": "ORD202401010001",
      "usageTime": "2024-01-01T14:30:00Z",
      "usageQuantity": 1
    }
  ]
}
```

#### 字段说明

**外层字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| dataType | string | 是 | 固定值：usage |
| batchId | string | 是 | 批次ID |
| timestamp | string | 是 | 发送时间，ISO8601格式 |
| usages | array | 是 | 使用记录数组 |

**usages数组内字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 关联的订单号 |
| usageTime | string | 是 | 使用时间，ISO8601格式 |
| usageQuantity | int | 是 | 使用数量 |

---

### 3.3 订单退单信息数据

#### 请求体示例

```json
{
  "dataType": "refund",
  "batchId": "batch-20240101-003",
  "timestamp": "2024-01-01T10:00:00Z",
  "refunds": [
    {
      "orderNo": "ORD202401010001",
      "refundTime": "2024-01-01T16:00:00Z",
      "refundQuantity": 1
    }
  ]
}
```

#### 字段说明

**外层字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| dataType | string | 是 | 固定值：refund |
| batchId | string | 是 | 批次ID |
| timestamp | string | 是 | 发送时间，ISO8601格式 |
| refunds | array | 是 | 退单记录数组 |

**refunds数组内字段：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| orderNo | string | 是 | 关联的订单号 |
| refundTime | string | 是 | 退单时间，ISO8601格式 |
| refundQuantity | int | 是 | 退单数量 |

---

## 四、数据加密说明

### 4.1 加密字段

以下字段采用AES加密：

| 数据类型 | 加密字段 | 说明 |
|---------|---------|------|
| 订单信息 | phone | 用户手机号 |

### 4.2 加密算法

- **算法**：AES-256-CBC
- **密钥**：32字节（256位）
- **初始向量(IV)**：16字节（128位）
- **填充**：PKCS7
- **输出**：Base64编码

### 4.3 解密示例

我方会提供密钥(Key)和初始向量(IV)，贵方需要按以下方式解密：

**解密步骤：**

1. 将Base64字符串解码为字节数组
2. 使用AES-256-CBC算法解密
3. 将解密后的字节数组转为UTF-8字符串

**各语言解密参考：**

#### Java示例

```java
import javax.crypto.Cipher;
import javax.crypto.spec.IvParameterSpec;
import javax.crypto.spec.SecretKeySpec;
import java.util.Base64;

public class AesDecrypt {
    public static String decrypt(String encrypted, String key, String iv) throws Exception {
        Cipher cipher = Cipher.getInstance("AES/CBC/PKCS5Padding");
        SecretKeySpec keySpec = new SecretKeySpec(key.getBytes("UTF-8"), "AES");
        IvParameterSpec ivSpec = new IvParameterSpec(iv.getBytes("UTF-8"));
        cipher.init(Cipher.DECRYPT_MODE, keySpec, ivSpec);
        byte[] decrypted = cipher.doFinal(Base64.getDecoder().decode(encrypted));
        return new String(decrypted, "UTF-8");
    }
}
```

#### Python示例

```python
from Crypto.Cipher import AES
import base64

def decrypt(encrypted, key, iv):
    cipher = AES.new(key.encode('utf-8'), AES.MODE_CBC, iv.encode('utf-8'))
    decrypted = cipher.decrypt(base64.b64decode(encrypted))
    return decrypted.decode('utf-8').rstrip('\x00').rstrip()
```

#### Node.js示例

```javascript
const crypto = require('crypto');

function decrypt(encrypted, key, iv) {
    const decipher = crypto.createDecipheriv('aes-256-cbc', key, iv);
    let decrypted = decipher.update(encrypted, 'base64', 'utf8');
    decrypted += decipher.final('utf8');
    return decrypted;
}
```

### 4.4 密钥获取

加密密钥(Key)和初始向量(IV)，请注意保密。

"Key": "PR87ARAGbZjBqHifNhT2ve96lC2R38XC",
"IV": "u0GF47E3KSsnkJK8"
---

## 五、响应规范

### 5.1 成功响应

贵方接口成功处理数据后，应返回以下格式的响应：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "receivedCount": 100
  }
}
```

**响应字段说明：**

| 字段名 | 类型 | 必填 | 说明 |
|-------|------|------|------|
| code | int | 是 | 状态码，200表示成功 |
| message | string | 是 | 状态说明 |
| data | object | 否 | 响应数据 |
| data.receivedCount | int | 否 | 实际接收处理的数据条数 |

### 5.2 失败响应

处理失败时，应返回：

```json
{
  "code": 500,
  "message": "失败原因描述",
  "data": null
}
```

**HTTP状态码：**

- `200` - 处理成功
- `400` - 请求参数错误
- `401` - 未授权（如需认证）
- `500` - 服务器内部错误

---

## 六、对接流程

### 6.1 对接步骤

```
1. 贵方提供接口地址和认证方式（如有）
        ↓
2. 我方配置对接参数
        ↓
3. 双方确认数据加密密钥
        ↓
4. 我方发送测试数据
        ↓
5. 贵方验证数据格式和解密逻辑
        ↓
6. 确认对接成功
```

### 6.2 对接检查清单

贵方需要确认以下内容：

- [ ] 已提供接口地址
- [ ] 已实现数据接收接口
- [ ] 已实现手机号解密功能
- [ ] 已实现数据存储逻辑
- [ ] 已实现响应格式
- [ ] 已验证数据格式正确性
- [ ] 已验证解密结果正确性

---


## 七、常见问题

### Q1: 数据推送频率是多少？

A: 每5-10分钟推送一次，每次推送最多100条记录。

### Q2: 如果接口暂时不可用怎么办？

A: 我方会记录失败日志，并在下次同步时重试。贵方接口恢复后，历史数据会自动补推。

### Q3: 如何处理重复数据？

A: 建议贵方根据orderNo进行幂等性处理，重复订单号的数据可忽略或更新。

### Q4: 时间戳的时区是什么？

A: 所有时间戳均为UTC时间（带Z后缀），如：`2024-01-01T10:00:00Z`

### Q5: 订单号是否唯一？

A: 是的，orderNo在系统中是唯一的标识符。







**文档结束**
