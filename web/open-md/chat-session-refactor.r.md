# AI Chat Session & UI Refactor

## 任务描述
1.  **UI 优化**：修复输入框上边距过大导致消息被遮挡的问题。
2.  **初始状态调整**：移除默认欢迎语，实现仅在无消息时显示的主脑中枢空状态。
3.  **多会话管理**：实现会话列表 Drawer，支持新建会话和切换历史会话。

## 任务步骤
1.  [x] 修改 `chatService.js`：
    -   添加 `getSessions`, `saveSession` 等本地会话管理方法。
2.  [x] 修改 `ChatBIView.jsx`：
    -   移除硬编码的初始消息。
    -   实现空状态 UI（Logo + 引导语 + 快捷指令）。
    -   添加右侧 Drawer 组件展示历史会话。
    -   实现会话切换逻辑（点击列表项 -> 加载历史记录）。
    -   优化输入框容器样式（增加 padding-bottom 防止遮挡）。
3.  [x] 更新 `module.md`：
    -   更新组件功能描述。

## 进度跟踪
- [x] UI 样式修复
- [x] 会话管理功能
- [x] 文档更新
