/**
 * @title 上下文角色 Context Role
 * @description 对话消息的角色类型枚举。
 * @keywords-cn 角色, 上下文, 枚举
 * @keywords-en role, context, enum
 */
export enum ContextRole {
  System = 'system',
  User = 'user',
  Assistant = 'assistant',
}
